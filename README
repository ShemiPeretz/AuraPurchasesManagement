
* install correct keda version!
    * # 1. Create the KEDA namespace
        kubectl create namespace keda
        # 2. Apply the official KEDA release manifest
        kubectl apply -f https://github.com/kedacore/keda/releases/download/v2.13.1/keda-2.13.1.yaml
    * # Apply the official KEDA HTTP Add-on v0.7.0 release manifest (or latest stable version)
        kubectl apply -f https://github.com/kedacore/http-add-on/releases/download/v0.7.0/keda-add-ons-http-0.7.0.yaml

* apply the k8s

* port-forward the facing service